// The public deploy origin — single source of truth in code. index.html,
// robots.txt and sitemap.xml repeat the same literal (they cannot import TS),
// so one grep for `wireframe2prompt.com` finds every spot at once.
export const SITE_URL = "https://wireframe2prompt.com";
